<%= form_with(model: vacation_hours_log, local: true) do |form| %>
  <% if vacation_hours_log.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vacation_hours_log.errors.count, "error") %> prohibited this vacation_hours_log from being saved:</h2>

      <ul>
      <% vacation_hours_log.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :vacation_hours_entry %>
    <%= form.text_field :vacation_hours_entry, id: :vacation_hours_log_vacation_hours_entry %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.date_select :date, id: :vacation_hours_log_date %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, id: :vacation_hours_log_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
