json.extract! vacation_hours_event, :id, :vacation_hours_used, :vacation_hours_gained, :vacation_hours_adj_date, :created_at, :updated_at
json.url vacation_hours_event_url(vacation_hours_event, format: :json)
